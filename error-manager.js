// error-manager.js
export class ErrorManager {
    static log(error, context = {}) {
        // لاگ کردن خطا
    }
    
    static notify(error, level = 'error') {
        // نمایش به کاربر
    }
    
    static recover(error) {
        // تلاش برای ریکاوری
    }
}
